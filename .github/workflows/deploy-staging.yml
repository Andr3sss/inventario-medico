name: Desplegar staging

on:
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  staging:
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: staging
      cloudflare_branch: staging
    secrets: inherit
